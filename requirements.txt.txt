# Core scientific and plotting libraries
pandas>=2.0.0
matplotlib>=3.8.0
numpy>=1.26.0

# Machine learning / YOLO
ultralytics>=8.0.200

# Image processing
Pillow>=10.0.0
piexif>=1.1.3

# GUI
FreeSimpleGUI>=5.0.0

# Web map generation
folium>=0.16.0

# KML creation and XML handling
lxml>=4.9.3
pykml>=0.2.0

# Optional utilities
requests>=2.31.0
opencv-python>=4.9.0.80

# For HTML and web preview
webbrowser  # built-in (no install needed, but kept for clarity)

# Optional: prevent OpenMP runtime conflicts (for YOLO/Ultralytics on Windows)
# Set this environment variable manually if you get libiomp5md.dll issues:
# KMP_DUPLICATE_LIB_OK=TRUE
